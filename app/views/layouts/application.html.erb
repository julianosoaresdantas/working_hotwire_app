<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "My Awesome Blog" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# Include Rails' default stylesheets and JavaScripts %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <script src="https://cdn.tailwindcss.com"></script>

    <style>
      /* Custom font for a modern look */
      body {
        /* Assuming 'Inter' font is imported via Tailwind config or another method */
        font-family: "Inter", sans-serif;
      }
    </style>

    <%# Optional: PWA and favicons - uncomment if you are setting these up %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= yield :head %> <%# For specific head content from individual views %>
  </head>

  <body class="bg-gray-100 min-h-screen flex flex-col">
    <div id="flash" class="w-full max-w-4xl mx-auto px-4 mt-4">
      <%= render "layouts/flash" %>
    </div>

    <header class="bg-white shadow-sm py-4">
      <div class="max-w-4xl mx-auto px-4 flex justify-between items-center">
        <div class="text-2xl font-bold text-gray-800">
          <%= link_to "My Awesome Blog", root_path, class: "hover:text-blue-600" %>
        </div>
        <nav class="space-x-4">
          <% if user_signed_in? %>
            <span class="text-gray-700">Logged in as <%= current_user.email %></span>
            <%= link_to "Edit profile", edit_user_registration_path, class: "text-blue-600 hover:underline" %>
            <%= button_to "Logout", destroy_user_session_path, method: :delete, class: "text-blue-600 hover:underline" %>
          <% else %>
            <%= link_to "Sign up", new_user_registration_path, class: "text-blue-600 hover:underline" %>
            <%= link_to "Login", new_user_session_path, class: "text-blue-600 hover:underline" %>
          <% end %>
        </nav>
      </div>
    </header>

    <main class="flex-grow container mx-auto px-4 py-8">
      <%= yield %>
    </main>

    <footer class="bg-gray-800 text-white py-4 mt-8">
      <div class="max-w-4xl mx-auto px-4 text-center text-sm">
        &copy; <%= Time.current.year %> My Awesome Blog. All rights reserved.
      </div>
    </footer>
  </body>
</html>
