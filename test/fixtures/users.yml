# test/fixtures/users.yml
one:
  email: user_one@example.com
  encrypted_password: <%= User.new.send(:password_digest, 'password') %> # Use a real password hash
  # Add other attributes if your User model has them
